language: node_js

os: linux

node_js:
  - lts/erbium
  - lts/dubnium

cache:
  directories:
    - $HOME/.npm

jobs:
  include:
    - name: "Google Cloud Environment (Node 10.15.3)"
      os: linux
      node_js: 10.15.3
    - name: "node (With coverage)"
      node_js: node
      after_success: cat coverage/lcov.info | npx coveralls
    - name: "Lint and Typecheck"
      node_js: node
      script: npm run lint && npm run typecheck
    - name: "Build and Docs (linux)"
      node_js: node
      os: linux
      script: npm run build && npm run docs
    - name: "Build and Docs (osx)"
      node_js: node
      os: osx
      script: npm run build && npm run docs
    - name: "Build and Docs (windows)"
      node_js: node
      os: windows
      script: npm run build && npm run docs
